# dependencies
node_modules/

# build output
dist/
*.tsbuildinfo

# runtime ledger state
.tab/

# credentials — never commit
.env
.env.*
# …but the template carries variable NAMES only, so it is tracked.
!.env.example
credentials
credentials.json
*.key
*.pem
*.p12

# captured CLI --help output: another project's docs, and reproducible in one
# command. Regenerate with `npm run capture:help` instead of committing it.
docs/cli-help/

# logs / os
*.log
.DS_Store
demo/logs/
