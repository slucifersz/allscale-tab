# dependencies
node_modules/

# build output
dist/
*.tsbuildinfo

# runtime ledger state
.tab/

# credentials — never commit
.env
.env.*
# …but the template carries variable NAMES only, so it is tracked.
!.env.example
credentials
credentials.json
*.key
*.pem
*.p12

# internal task brief / working notes: kept locally, never published.
the design brief

# captured CLI --help output: the full command surface of an unreleased
# product. Reproduce locally with `npm run capture:help`; never commit it.
docs/cli-help/

# logs / os
*.log
.DS_Store
demo/logs/
